import ballSize from './ballSize';
import ballSpeed from './ballSpeed';
import color from './color';
import fps from './fps';
import paddleHeight from './paddleHeight';
import paddleWidth from './paddleWidth';

export default {
	ballSize,
	ballSpeed,
	color,
	fps,
	paddleHeight,
	paddleWidth,
};
